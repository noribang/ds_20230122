// Binary search
// Log(n) time.
// Sorted.

function binarySearch(arr, val) {
    // 
    let min = 0;
    let max = arr.length - 1;
    // Cut search in half on each iteration.
    while (min <= max) {
        // Variable for middle.
        let middle = Math.floor((min + max) / 2);
        // Get middle element in array.
        let currentElement = arr[middle];

    }

}